//What is the difference between "a" and 'a'?
Answer: The expression "a" is a string literal, which is stored in the memory as an
array of two characters, the 'a' and '\0'. On the other hand, the expression 'a' is
just the single 'a' character and it is represented by its ASCII code.
