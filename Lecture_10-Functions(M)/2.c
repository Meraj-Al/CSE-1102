//Use of Library Function in C
#include<stdio.h>
#include<math.h>

int main()
{
    double Number;
    Number = 49;
    double squareRoot = sqrt(Number);
    printf("The Square root of %.2f = %.2f", Number, squareRoot);
    return 0;
}
